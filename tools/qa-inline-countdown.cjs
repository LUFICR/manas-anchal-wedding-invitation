const {chromium}=require('C:/Users/Ayush/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'chrome'});for(const [width,height] of [[390,844],[393,852],[430,932]]){const p=await b.newPage({viewport:{width,height}});await p.goto('http://127.0.0.1:5174');await p.locator('.letter-wax').click();await p.waitForTimeout(4200);assert.equal(await p.locator('#countdown').count(),0);await p.locator('.tap-reveal').click();const y=await p.evaluate(()=>scrollY);await p.waitForTimeout(3000);assert.equal(await p.locator('#countdown').count(),0);await p.waitForTimeout(2300);assert.equal(await p.locator('#scratch-date #countdown [role=timer]').count(),1);assert.equal(await p.locator('section#countdown').count(),0);assert.equal(await p.evaluate(()=>scrollY),y);assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth>innerWidth),false);await p.locator('#countdown').scrollIntoViewIfNeeded();await p.screenshot({path:`tools/inline-countdown-${width}.png`});await p.close();}await b.close();console.log('Three mobile viewports: gated countdown, delayed entrance, same scene, no forced scroll or overflow passed.');})();
